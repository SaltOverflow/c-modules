module invalidDefinitions;

// function prototypes don't actually define the symbol 
export void (*signal(int, void (*fp)(int)))(int);

// extern declarations don't actually define the symbol
export extern int i;
