module defineInsideFunction;

void (*fnptr)(long double ld);

void foo(double d) {
    struct S { long long ll; } s = { 42 };
    struct S ss;
    union U { float f; char a[3]; } u = { .f = 4.2 };
    enum E { A = 1, B = 2 } e = A;
    typedef int I;
    I i = i;

    d; s; u; e; i;

    for (long l = 0; l<4; ++l) {
        long l = l;  // this is self-assignment, not the previous l
    }
    for (d=0;;) {}
}
